<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustNothing"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="stepovr" />
            </intent-filter>

            <!-- Android App Links (https) verified via /.well-known/assetlinks.json -->
            <!-- TODO: replace stepovr.com with final production domain once purchased -->
            <!-- Scoped to specific paths so marketing pages (privacy, blog, etc.) stay in the browser -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="stepovr.com" android:pathPrefix="/join" />
                <data android:scheme="https" android:host="stepovr.com" android:pathPrefix="/duel" />
                <data android:scheme="https" android:host="stepovr.com" android:pathPrefix="/battle-royale" />
                <data android:scheme="https" android:host="stepovr.com" android:pathPrefix="/logo-quiz" />
                <data android:scheme="https" android:host="www.stepovr.com" android:pathPrefix="/join" />
                <data android:scheme="https" android:host="www.stepovr.com" android:pathPrefix="/duel" />
                <data android:scheme="https" android:host="www.stepovr.com" android:pathPrefix="/battle-royale" />
                <data android:scheme="https" android:host="www.stepovr.com" android:pathPrefix="/logo-quiz" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-7781323448253047~6079077395"
        />

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
